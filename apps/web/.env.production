# Production builds (e.g. the desktop bundle, whose webview is not same-origin
# with the API) target the embedded server's absolute origin. The dev server
# leaves VITE_API_BASE unset so the app uses same-origin `/api` via the proxy.
VITE_API_BASE=http://127.0.0.1:3000
